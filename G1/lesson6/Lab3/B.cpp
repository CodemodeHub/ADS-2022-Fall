#include <iostream>
using namespace std;

int main()
{
	int n, k;
	cin >> n >> k;
	int ar[n];
	for (int i = 0; i < n; i++)
	{
		cin >> ar[i];
	}
	unsigned long long left = 0, right = 1e9, mid, cnt, sum;
	while (left < right) {
		mid = left + (right - left) / 2;
		cnt = 1;
		sum = 0;
		
	}

	return 0;
}